#include<bits/stdc++.h>
using namespace std;

#define vi vector<int>
#define pii pair<int,int>
#define rep(i,a,b) for(int i=a;i<b;i++)
#define ff first
#define ss second

int main(){
    int n;
    cin>>n;
    vector<pair<int,int>> elements(n);
    for (int i = 0; i < n; i++)
    {
        int u,v;
        cin>>u>>v;
        elements.push_back({u,v});
    }
    
    

    return 0;
}